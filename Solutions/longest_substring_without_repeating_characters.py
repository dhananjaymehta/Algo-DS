# Given a string, find the length of the longest substring without repeating characters.
# "abcabcbb" is "abc", which the length is 3.
# "bbbbbb" is "b", length 1

def longest_substring_without_repeatig_characters(string):
    ma
    while len
